Ensure you have a valid directory structure using the files in `src`

For example:
```
playbooks/
roles/
  primgr/
    templates/
      (put the primgr template files from `src` here)
  pgenable/
    templates/     
      ..


```

Requirements
------------
